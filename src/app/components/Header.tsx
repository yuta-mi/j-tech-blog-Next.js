import { Title } from "./Title";

export const Header = () => {
  return (
    <header className="bg-base-100">
      <div className="container mx-auto">
        <Title />
      </div>
    </header>
  );
};